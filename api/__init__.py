"""FastAPI application package.

The app is built in :mod:`api.main`; import it from there
(``from api.main import app``) so that merely importing ``api.schemas`` does not
construct the application as a side effect.
"""
